name: C++ build (with CMake) and run tests on multiple platforms

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-gcc:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: true
      matrix:
        exceptions: [
          {
            job_name: "Exept",
            name: "",
            cmake: -DASYNC_CORO_NO_EXCEPTIONS=FALSE
          },
          {
            job_name: "NoExept",
            name: "-noexcept",
            cmake: -DASYNC_CORO_NO_EXCEPTIONS=TRUE
          },
        ]
        sanitizers: [
          {
            job_name: "",
            name: "",
            cmake: -DASYNC_CORO_ASAN_ENABLED=FALSE
          },
          {
            job_name: "TSAN, ",
            name: "-tsan",
            cmake: -DASYNC_CORO_TSAN_ENABLED=TRUE
          },
          {
            job_name: "ASAN, ",
            name: "-asan",
            cmake: -DASYNC_CORO_ASAN_ENABLED=TRUE
          }
        ]

    name: GCC (x86) [${{matrix.sanitizers.job_name}}${{matrix.exceptions.job_name}}]

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive
        
    - name: Setup GCC 13
      uses: Dup4/actions-setup-gcc@v1
      with:
        version: 13
      
    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=g++
        -DCMAKE_C_COMPILER=gcc
        -DCMAKE_BUILD_TYPE=RelWithDebInfo
        -DASYNC_CORO_TESTS_ENABLED=TRUE
        ${{ matrix.exceptions.cmake }}
        ${{ matrix.sanitizers.cmake }}
        -S ${{ github.workspace }}

    - name: Build
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --parallel

    - name: Test
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      timeout-minutes: 10
      run: |
        chmod +x ${{ github.workspace }}/.github/scripts/run_tests.sh
        ${{ github.workspace }}/.github/scripts/run_tests.sh "${{ steps.strings.outputs.build-output-dir }}"
  
    - name: Signal cleanup on job cancellation
      if: cancelled()
      run: |
        chmod +x ${{ github.workspace }}/.github/scripts/signal_cleanup.sh
        ${{ github.workspace }}/.github/scripts/signal_cleanup.sh "${{ steps.strings.outputs.build-output-dir }}"
  
    - name: Archive run logs
      if: always()
      uses: actions/upload-artifact@v6
      with:
        name: gcc-run-logs${{matrix.sanitizers.name}}${{matrix.exceptions.name}}
        path: ${{ steps.strings.outputs.build-output-dir }}/Testing/Temporary/LastTest.log
        if-no-files-found: ignore

  build-clang:
    strategy:
      fail-fast: true
      matrix:
        os: [
          {
            job_name: "x86",
            name: ubuntu-latest
          },
          {
            job_name: "arm",
            name: ubuntu-24.04-arm
          }
        ]
        exceptions: [
          {
            job_name: "Exept",
            name: "",
            cmake: -DASYNC_CORO_NO_EXCEPTIONS=FALSE
          },
          {
            job_name: "NoExept",
            name: "-noexcept",
            cmake: -DASYNC_CORO_NO_EXCEPTIONS=TRUE
          },
        ]
        sanitizers: [
          {
            job_name: "",
            name: "",
            cmake: -DASYNC_CORO_ASAN_ENABLED=FALSE
          },
          {
            job_name: "TSAN, ",
            name: "-tsan",
            cmake: -DASYNC_CORO_TSAN_ENABLED=TRUE
          },
          {
            job_name: "ASAN, ",
            name: "-asan",
            cmake: -DASYNC_CORO_ASAN_ENABLED=TRUE
          }
        ]
    
    runs-on: ${{matrix.os.name}}

    name: Clang (${{matrix.os.job_name}}) [${{matrix.sanitizers.job_name}}${{matrix.exceptions.job_name}}]

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Install Clang 20
      run: |
       wget https://apt.llvm.org/llvm.sh
       chmod +x ./llvm.sh
       sudo ./llvm.sh 20

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=clang++-20
        -DCMAKE_C_COMPILER=clang-20
        -DCMAKE_BUILD_TYPE=RelWithDebInfo
        -DASYNC_CORO_TESTS_ENABLED=TRUE
        ${{ matrix.sanitizers.cmake }}
        ${{ matrix.exceptions.cmake }}
        -S ${{ github.workspace }}

    - name: Build
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --parallel

    - name: Test
      timeout-minutes: 10
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      run: |
        chmod +x ${{ github.workspace }}/.github/scripts/run_tests.sh
        ${{ github.workspace }}/.github/scripts/run_tests.sh "${{ steps.strings.outputs.build-output-dir }}"
    
    - name: Signal cleanup on job cancellation
      if: cancelled()
      run: |
        chmod +x ${{ github.workspace }}/.github/scripts/signal_cleanup.sh
        ${{ github.workspace }}/.github/scripts/signal_cleanup.sh "${{ steps.strings.outputs.build-output-dir }}"
    
    - name: Archive run logs
      if: always()
      uses: actions/upload-artifact@v6
      with:
        name: ${{matrix.os.name}}-clang-run-logs${{matrix.sanitizers.name}}${{matrix.exceptions.name}}
        path: ${{ steps.strings.outputs.build-output-dir }}/Testing/Temporary/LastTest.log
        if-no-files-found: ignore
    
  build-msvc:
    runs-on: windows-latest

    strategy:
      fail-fast: true
      matrix:
        exceptions: [
          {
            job_name: "Exept",
            name: "",
            cmake: -DASYNC_CORO_NO_EXCEPTIONS=FALSE
          },
          {
            job_name: "NoExept",
            name: "-noexcept",
            cmake: -DASYNC_CORO_NO_EXCEPTIONS=TRUE
          },
        ]
        sanitizers: [
          {
            job_name: "",
            name: "",
            cmake: -DASYNC_CORO_ASAN_ENABLED=FALSE
          }
        ]

    name: MsBuild (x86) [${{matrix.sanitizers.job_name}}${{matrix.exceptions.job_name}}]

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_CXX_COMPILER=cl
        -DCMAKE_C_COMPILER=cl
        -DCMAKE_BUILD_TYPE=RelWithDebInfo
        -DASYNC_CORO_TESTS_ENABLED=TRUE
        ${{ matrix.exceptions.cmake }}
        ${{ matrix.sanitizers.cmake }}
        -S ${{ github.workspace }}

    - name: Build
      # Build your program with the given configuration. Note that --config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --config RelWithDebInfo --parallel

    - name: Test
      timeout-minutes: 10
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      # Execute tests defined by the CMake configuration. Note that --build-config is needed because the default Windows generator is a multi-config generator (Visual Studio generator).
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
      run: ctest --build-config RelWithDebInfo --output-on-failure
    
    - name: Archive run logs
      if: always()
      uses: actions/upload-artifact@v6
      with:
        name: msvc-run-logs${{matrix.sanitizers.name}}${{matrix.exceptions.name}}
        path: ${{ steps.strings.outputs.build-output-dir }}/Testing/Temporary/LastTest.log

  build-android:
    if: false  # Temporarily disabled - testing clang only
    runs-on: macos-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: recursive

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: 17
          distribution: 'adopt'
          cache: 'gradle'
        
      - name: Run connected tests
        uses: ReactiveCircus/android-emulator-runner@v2
        with:
          api-level: 30
          target: google_apis
          arch: x86_64
          working-directory: ./tests/AndroidGoogleTest
          script: ./gradlew connectedCheck

  build-mac:
    strategy:
      fail-fast: true
      matrix:
        os: [
          {
            job_name: "x86",
            name: macos-15-intel
          },
          {
            job_name: "arm",
            name: macos-26
          }
        ]
        exceptions: [
          {
            job_name: "Exept",
            name: "",
            cmake: -DASYNC_CORO_NO_EXCEPTIONS=FALSE
          },
          {
            job_name: "NoExept",
            name: "-noexcept",
            cmake: -DASYNC_CORO_NO_EXCEPTIONS=TRUE
          },
        ]
        sanitizers: [
          {
            job_name: "",
            name: "",
            cmake: -DASYNC_CORO_ASAN_ENABLED=FALSE
          },
          {
            job_name: "ASAN, ",
            name: "-asan",
            cmake: -DASYNC_CORO_ASAN_ENABLED=TRUE
          }
        ]

    runs-on: ${{ matrix.os.name }}

    name: Xcode (${{matrix.os.job_name}}) [${{matrix.sanitizers.job_name}}${{matrix.exceptions.job_name}}]

    steps:
    - uses: actions/checkout@v6
      with:
        submodules: recursive

    - name: Set reusable strings
      # Turn repeated input strings (such as the build output directory) into step outputs. These step outputs can be used throughout the workflow file.
      id: strings
      shell: bash
      run: |
        echo "build-output-dir=${{ github.workspace }}/build" >> "$GITHUB_OUTPUT"

    - name: Configure CMake
      # Configure CMake in a 'build' subdirectory. `CMAKE_BUILD_TYPE` is only required if you are using a single-configuration generator such as make.
      # See https://cmake.org/cmake/help/latest/variable/CMAKE_BUILD_TYPE.html?highlight=cmake_build_type
      run: >
        cmake -B ${{ steps.strings.outputs.build-output-dir }}
        -DCMAKE_BUILD_TYPE=RelWithDebInfo
        -DASYNC_CORO_TESTS_ENABLED=TRUE
        ${{ matrix.exceptions.cmake }}
        ${{ matrix.sanitizers.cmake }}
        -S ${{ github.workspace }}

    - name: Build
      run: cmake --build ${{ steps.strings.outputs.build-output-dir }} --parallel

    - name: Test
      timeout-minutes: 10
      working-directory: ${{ steps.strings.outputs.build-output-dir }}
      run: |
        chmod +x ${{ github.workspace }}/.github/scripts/run_tests.sh
        ${{ github.workspace }}/.github/scripts/run_tests.sh "${{ steps.strings.outputs.build-output-dir }}"
    
    - name: Signal cleanup on job cancellation
      if: cancelled()
      run: |
        chmod +x ${{ github.workspace }}/.github/scripts/signal_cleanup.sh
        ${{ github.workspace }}/.github/scripts/signal_cleanup.sh "${{ steps.strings.outputs.build-output-dir }}"
    
    - name: Archive run logs
      if: always()
      uses: actions/upload-artifact@v6
      with:
        name: ${{matrix.os}}-run-logs${{matrix.sanitizers.name}}${{matrix.exceptions.name}}
        path: ${{ steps.strings.outputs.build-output-dir }}/Testing/Temporary/LastTest.log
        if-no-files-found: ignore

  dispatch-pr:
    needs: [build-gcc, build-clang, build-msvc, build-mac]
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Trigger gatekeeper for PR approval
        uses: peter-evans/repository-dispatch@v4
        with:
          event-type: check-finished
          client-payload: |
            {
              "sha": "${{ github.event.pull_request.head.sha }}",
              "future_sha": "${{ github.sha }}",
              "pr": "${{ github.event.pull_request.number }}"
            }

        