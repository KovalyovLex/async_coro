name: PR Auto Approve Gatekeeper

on:
  repository_dispatch:
    types: [check-finished]

jobs:
  gatekeeper:
    runs-on: ubuntu-latest
    steps:
      - name: Auto approve if repo owner
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.PR_APPROVER_TOKEN }}
          script: |
            const sha = context.payload.client_payload.sha;
            const prNumber = context.payload.client_payload.pr;

            if (!prNumber) {
              console.log("🚫 No PR number found — skipping");
              return;
            }

            // Get PR info
            const { data: pr } = await github.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber
            });

            const prAuthor = pr.user.login;
            console.log(`PR author: ${prAuthor}`);

            // Check if PR author is a collaborator with write/admin permissions
            try {
              const { data: perm } = await github.repos.getCollaboratorPermissionLevel({
                owner: context.repo.owner,
                repo: context.repo.repo,
                username: prAuthor
              });

              console.log(`Permission level for ${prAuthor}: ${perm.permission}`);

              if (perm.permission === "write" || perm.permission === "admin") {
                const combined = await github.repos.getCombinedStatusForRef({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  ref: sha
                });

                if (combined.data.state === "success") {
                  console.log("✅ All checks passed & PR author is maintainer — approving PR");
                  await github.pulls.createReview({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    pull_number: prNumber,
                    event: "APPROVE"
                  });
                } else {
                  console.log("⏳ Checks not all passed yet");
                }
              } else {
                console.log("👥 PR author is not a maintainer — skipping auto-approve");
              }
            } catch (err) {
              console.log("⚠️ Could not check collaborator permissions:", err.message);
            }
